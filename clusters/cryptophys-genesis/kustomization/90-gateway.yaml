apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: 90-gateway
  namespace: flux-system
spec:
  suspend: true
  interval: 1h
  path: ./platform/infrastructure/gateway
  prune: false
  sourceRef:
    kind: GitRepository
    name: 90-gateway
  dependsOn:
    - name: 90-gateway
    - name: 90-gateway
    - name: 90-gateway
