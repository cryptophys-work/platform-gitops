apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: 01-namespaces
  namespace: flux-system
  annotations:
    cryptophys.io/domain: platform
    cryptophys.io/layer: platform
    cryptophys.io/managed-by: flux
    reconcile.fluxcd.io/requestedAt: "2026-04-12T00:01:28Z"
spec:
  suspend: false   # Unsuspend after 00-crds is Applied
  interval: 10m
  force: true     # Apply label updates to existing namespaces
  prune: false    # Never prune namespaces
  wait: false
  timeout: 5m
  path: ./platform/infrastructure/namespaces
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: 00-crds
