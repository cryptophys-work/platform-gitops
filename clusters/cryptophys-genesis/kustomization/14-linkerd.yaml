apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: 14-linkerd
  namespace: flux-system
spec:
  suspend: true
  dependsOn:
    - name: 11-controllers
    - name: 35-spire # Must wait for SPIRE to provide identity
  interval: 1h
  path: ./platform/infrastructure/linkerd
  prune: true
  sourceRef:
    kind: GitRepository
    name: platform-repo
