apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: security-trivy-operator
  namespace: security-system
spec:
  interval: 1h
  install:
    remediation:
      retries: 3
    createNamespace: false
    crds: CreateReplace
  upgrade:
    remediation:
      retries: 3
    crds: CreateReplace
  chart:
    spec:
      chart: trivy-operator
      sourceRef:
        kind: HelmRepository
        name: aquasecurity
        namespace: flux-system
  values:
    replicaCount: 1
    trivy:
      # ClientServer mode — operator tidak spawn pod untuk download DB
      # Semua scan di-forward ke trivy-server di security-system
      mode: ClientServer
      serverURL: "http://trivy-server.security-system:4954"
      ignoreUnfixed: true
      dbRepository: mirror.gcr.io/aquasec/trivy-db
      resources:
        requests:
          cpu: 100m
          memory: 100M
        limits:
          cpu: 500m
          memory: 500M
    serviceMonitor:
      enabled: true
    tolerations: []
    scanJob:
      tolerations: []
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        cpu: 250m
        memory: 512Mi
