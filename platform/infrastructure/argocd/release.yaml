apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: argocd
  namespace: apps-system
spec:
  releaseName: argocd
  interval: 1h
  timeout: 30m
  chart:
    spec:
      chart: argo-cd
      version: 9.4.1
      sourceRef:
        kind: HelmRepository
        name: argoproj
        namespace: flux-system
  install:
    timeout: 30m
    remediation:
      retries: 3
  upgrade:
    timeout: 30m
    force: true
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    crds:
      install: true
      keep: true
    configs:
      params:
        server.insecure: "true"
      cm:
        resource.customizations.ignoreDifferences.argoproj.io_Application: |
          jqPathExpressions:
            - '.metadata.annotations."argocd.argoproj.io/tracking-id"'
            - '.metadata.annotations."cryptophys.io/gitops-source"'
        resource.customizations.health.gateway.envoyproxy.io_SecurityPolicy: |
          hs = {}
          hs.status = "Healthy"
          if obj.status ~= nil then
            for _, c in ipairs(obj.status.ancestors or {}) do
              for _, condition in ipairs(c.conditions or {}) do
                if condition.type == "Accepted" and condition.status == "False" then
                  hs.status = "Degraded"
                  hs.message = condition.message
                  return hs
                end
              end
            end
          end
          hs.message = "Policy accepted"
          return hs
    global:
      nodeSelector:
        cryptophys.io/tier: compute
      tolerations: []
    controller:
      replicas: 2
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          cpu: 1000m
          memory: 2Gi
    server:
      replicas: 2
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
        limits:
          cpu: 250m
          memory: 256Mi
    repoServer:
      replicas: 2
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          cpu: 500m
          memory: 512Mi
      # Certificate-based Git authentication using SPIRE-issued certificates
      env:
        - name: GIT_SSL_CERT
          value: /tmp/spiffe-certs/svid.pem
        - name: GIT_SSL_KEY
          value: /tmp/spiffe-certs/svid_key.pem
        - name: GIT_SSL_CAINFO
          value: /tmp/spiffe-certs/svid_bundle.pem
        - name: GIT_SSL_NO_VERIFY
          value: "false"
      volumes:
        - name: spiffe-workload-api
          csi:
            driver: csi.spiffe.io
            readOnly: true
        - name: spiffe-certs
          emptyDir:
            medium: Memory
            sizeLimit: 10Mi
        - name: spiffe-helper-config
          configMap:
            name: argocd-spiffe-helper-config
      volumeMounts:
        - name: spiffe-workload-api
          mountPath: /spiffe-workload-api
          readOnly: true
        - name: spiffe-certs
          mountPath: /tmp/spiffe-certs
      # Sidecar: spiffe-helper extracts certs from CSI volume to filesystem for Git client
      extraContainers:
        - name: spiffe-helper
          image: ghcr.io/spiffe/spiffe-helper:0.11.0
          imagePullPolicy: IfNotPresent
          args:
            - -config
            - /etc/spiffe-helper/helper.conf
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 999
          volumeMounts:
            - name: spiffe-workload-api
              mountPath: /spiffe-workload-api
              readOnly: true
            - name: spiffe-certs
              mountPath: /tmp/spiffe-certs
            - name: spiffe-helper-config
              mountPath: /etc/spiffe-helper
              readOnly: true
    applicationSet:
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
        limits:
          cpu: 250m
          memory: 256Mi
    redis:
      # Disable built-in Redis - using external HA cluster
      enabled: false
    externalRedis:
      host: argocd-redis-ha.apps-system.svc.cluster.local
      port: 6379
    notifications:
      resources:
        requests:
          cpu: 25m
          memory: 32Mi
        limits:
          cpu: 100m
          memory: 128Mi
    dex:
      enabled: false
