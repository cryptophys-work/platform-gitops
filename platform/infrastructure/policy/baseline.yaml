# default-deny-baseline removed: conflicted with platform-baseline-allow-internal
# (platform-baseline-allow-internal in cilium-baseline.yaml provides the actual allow policy)
# When kubeProxyReplacement=false, kube-proxy service VIPs are not Cilium endpoints
# and toEntities:[cluster] does not cover them, causing EPERM / context deadline exceeded
# for API server webhooks and inter-service connections.
