apiVersion: "cilium.io/v2"
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: "default-deny-baseline"
spec:
  endpointSelector: {}
  ingress: [{fromEntities: [host, cluster, kube-apiserver]}]
  egress: [{toEntities: [host, cluster, kube-apiserver]}]
