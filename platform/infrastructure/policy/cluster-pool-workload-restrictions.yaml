# DEPRECATED: storage-only workload restriction policy removed (2026-04-14)
# Replaced by daemonset=true:NoSchedule taint model
#
# New approach:
# - Campus and medulla nodes have daemonset=true:NoSchedule taint
# - Only pods with daemonset=true toleration can schedule there
# - No policy needed; taint enforcement is automatic
#
# See: /root/.claude/plans/storage-only-deprecation.md
