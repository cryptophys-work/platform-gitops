apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # release.yaml temporarily omitted — Flux prune removes HelmRelease so Helm uninstall clears
  # stale merged values; restore in follow-up commit with prune disabled again.
  - pool.yaml
