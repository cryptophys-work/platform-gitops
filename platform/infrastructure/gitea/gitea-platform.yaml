apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: gitops-gitea
  namespace: gitops-system
spec:
  interval: 30m
  releaseName: gitops-gitea
  timeout: 30m
  install:
    disableWait: true
    timeout: 30m
    remediation:
      retries: 3
  upgrade:
    disableWait: true
    timeout: 30m
    cleanupOnFail: true
    remediation:
      retries: 5
  postRenderers:
    - kustomize:
        patches:
          - target:
              kind: Deployment
              name: gitops-gitea
            patch: |
              - op: replace
                path: /spec/replicas
                value: 1
              - op: replace
                path: /spec/strategy
                value:
                  type: RollingUpdate
                  rollingUpdate:
                    maxUnavailable: 1
                    maxSurge: 0
              - op: replace
                path: /spec/template/spec/nodeSelector
                value:
                  workload.cryptophys.work/platform: "true"
              - op: replace
                path: /spec/template/spec/tolerations
                value:
                  - key: cryptophys.io/pool
                    operator: Equal
                    value: platform-ha
                    effect: NoSchedule
                  - key: node.cilium.io/agent-not-ready
                    operator: Exists
                    effect: NoSchedule
              - op: replace
                path: /spec/template/spec/affinity
                value:
                  nodeAffinity:
                    requiredDuringSchedulingIgnoredDuringExecution:
                      nodeSelectorTerms:
                        - matchExpressions:
                            - key: kubernetes.io/hostname
                              operator: In
                              values:
                                - nexus-144-91-103-10
                                - cerebellum-161-97-117-96
                                - thalamus-217-76-59-241
                  podAntiAffinity:
                    requiredDuringSchedulingIgnoredDuringExecution:
                      - labelSelector:
                          matchLabels:
                            app.kubernetes.io/name: gitea
                            app.kubernetes.io/instance: gitops-gitea
                        topologyKey: kubernetes.io/hostname
              - op: add
                path: /spec/template/spec/topologySpreadConstraints
                value:
                  - maxSkew: 1
                    topologyKey: kubernetes.io/hostname
                    whenUnsatisfiable: DoNotSchedule
                    labelSelector:
                      matchLabels:
                        app.kubernetes.io/name: gitea
                        app.kubernetes.io/instance: gitops-gitea
              - op: replace
                path: /spec/template/spec/containers/0/readinessProbe
                value:
                  tcpSocket:
                    port: http
                  initialDelaySeconds: 20
                  timeoutSeconds: 5
                  periodSeconds: 10
                  successThreshold: 1
                  failureThreshold: 6
              - op: replace
                path: /spec/template/spec/containers/0/livenessProbe
                value:
                  tcpSocket:
                    port: http
                  initialDelaySeconds: 60
                  timeoutSeconds: 5
                  periodSeconds: 15
                  successThreshold: 1
                  failureThreshold: 4
              - op: add
                path: /spec/template/spec/containers/0/startupProbe
                value:
                  tcpSocket:
                    port: http
                  initialDelaySeconds: 10
                  timeoutSeconds: 5
                  periodSeconds: 10
                  successThreshold: 1
                  failureThreshold: 30
          - target:
              kind: Deployment
              name: gitops-gitea
            patch: |
              - op: add
                path: /spec/template/spec/initContainers/0
                value:
                  name: wait-for-rwx-migration
                  image: ghcr.io/go-gitea/gitea:1.25.4-rootless
                  command:
                    - /bin/sh
                    - -ec
                    - |
                      until [ -f /data/.gitea-rwx-migrated ]; do
                        echo "waiting for gitea rwx migration marker"
                        sleep 5
                      done
                  resources:
                    requests:
                      cpu: 10m
                      memory: 16Mi
                    limits:
                      cpu: 100m
                      memory: 128Mi
                  volumeMounts:
                    - name: data
                      mountPath: /data
  chart:
    spec:
      chart: gitea
      version: 12.5.0
      sourceRef:
        kind: HelmRepository
        name: gitea-charts
        namespace: flux-system
  values:
    replicaCount: 1
    image:
      registry: ghcr.io
      repository: go-gitea/gitea
      tag: "1.25.4"
      rootless: true
    gitea:
      admin:
        username: gitea-admin
        email: gitea-admin@cryptophys.work
        existingSecret: gitea-admin-secret
        passwordMode: keepUpdated
      livenessProbe:
        enabled: true
        initialDelaySeconds: 120
        periodSeconds: 10
        failureThreshold: 10
      readinessProbe:
        enabled: true
        initialDelaySeconds: 60
        periodSeconds: 10
        failureThreshold: 10
      config:
        database:
          DB_TYPE: postgres
          HOST: postgres-apps-ha-rw.postgresql-system.svc.cluster.local:5432
          NAME: gitea
          USER: gitea
        cache:
          ADAPTER: redis
          HOST: redis://gitea-redis.gitops-system.svc.cluster.local:6379/0
        session:
          PROVIDER: redis
          PROVIDER_CONFIG: redis://gitea-redis.gitops-system.svc.cluster.local:6379/0
        queue:
          TYPE: redis
          CONN_STR: redis://gitea-redis.gitops-system.svc.cluster.local:6379/1
        server:
          ROOT_URL: https://gitea.cryptophys.work
          DOMAIN: gitea.cryptophys.work
          GIT_EXECUTABLE_PATH: /usr/bin/git
        webhook:
          ALLOWED_HOST_LIST: webhook.cryptophys.work,webhook-receiver.tekton-system.svc.cluster.local,webhook-receiver.tekton-system.svc,localhost,127.0.0.1
          DELIVER_COMMIT_FILE_INFO: "true"
          DELIVER_TIMEOUT: "60"
    extraEnv:
      - name: GITEA__WEBHOOK__ALLOWED_HOST_LIST
        value: webhook.cryptophys.work,webhook-receiver.tekton-system.svc.cluster.local,webhook-receiver.tekton-system.svc,localhost,127.0.0.1
      - name: GITEA__WEBHOOK__DELIVER_COMMIT_FILE_INFO
        value: "true"
      - name: GITEA__WEBHOOK__DELIVER_TIMEOUT
        value: "60"
      - name: GITEA__CACHE__ADAPTER
        value: redis
      - name: GITEA__CACHE__HOST
        value: gitea-redis.gitops-system.svc.cluster.local:6379
      - name: GITEA__SESSION__PROVIDER
        value: redis
      - name: GITEA__SESSION__PROVIDER_CONFIG
        value: redis://gitea-redis.gitops-system.svc.cluster.local:6379/0
      - name: GITEA__QUEUE__TYPE
        value: redis
      - name: GITEA__QUEUE__CONN_STR
        value: redis://gitea-redis.gitops-system.svc.cluster.local:6379/1
      - name: GITEA__DATABASE__USER
        valueFrom:
          secretKeyRef:
            name: gitea-db-credentials
            key: username
      - name: GITEA__DATABASE__PASSWD
        valueFrom:
          secretKeyRef:
            name: gitea-db-credentials
            key: password
      - name: GITEA_ADMIN_BOOTSTRAP_USERNAME
        valueFrom:
          secretKeyRef:
            name: gitea-admin-secret
            key: username
      - name: GITEA_ADMIN_BOOTSTRAP_PASSWORD
        valueFrom:
          secretKeyRef:
            name: gitea-admin-secret
            key: password
    strategy:
      type: RollingUpdate
    deployment:
      lifecycle:
        postStart:
          exec:
            command:
              - /bin/sh
              - -c
              - |
                if [ -n "${GITEA_ADMIN_BOOTSTRAP_USERNAME:-}" ] && [ -n "${GITEA_ADMIN_BOOTSTRAP_PASSWORD:-}" ]; then
                  n=0
                  until gitea admin user change-password \
                    --username "${GITEA_ADMIN_BOOTSTRAP_USERNAME}" \
                    --password "${GITEA_ADMIN_BOOTSTRAP_PASSWORD}" \
                    --must-change-password=false >> /tmp/bootstrap-auth.log 2>&1; do
                    n=$((n+1))
                    if [ "$n" -ge 12 ]; then
                      echo "[bootstrap-auth] failed to normalize ${GITEA_ADMIN_BOOTSTRAP_USERNAME}" >> /tmp/bootstrap-auth.log
                      exit 1
                    fi
                    sleep 5
                  done
                  echo "[bootstrap-auth] normalized ${GITEA_ADMIN_BOOTSTRAP_USERNAME}" >> /tmp/bootstrap-auth.log
                fi
    ingress:
      enabled: true
      className: nginx
      annotations:
        nginx.ingress.kubernetes.io/proxy-body-size: "512m"
      hosts:
        - host: gitea.cryptophys.work
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: platform-wildcard-tls
          hosts:
            - gitea.cryptophys.work
    service:
      http:
        type: ClusterIP
        port: 3000
        clusterIP: ""
    persistence:
      enabled: true
      create: false
      claimName: gitea-shared-storage-rwx
      size: 2Gi
      storageClass: longhorn
      accessModes:
        - ReadWriteOnce
    postgresql:
      enabled: false
    postgresql-ha:
      enabled: false
    redis:
      enabled: false
    valkey:
      enabled: false
    valkey-cluster:
      enabled: false
    resources:
      requests:
        cpu: 10m
        memory: 32Mi
      limits:
        cpu: 1000m     # increased from 600m for 280MB repo with concurrent git ops
        memory: 1Gi    # increased from 768Mi
    tolerations:
      - key: cryptophys.io/pool
        operator: Equal
        value: platform-ha
        effect: NoSchedule
      - key: node.cilium.io/agent-not-ready
        operator: Exists
        effect: NoSchedule
    nodeSelector:
      workload.cryptophys.work/platform: "true"
    affinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: kubernetes.io/hostname
                  operator: In
                  values:
                    - nexus-144-91-103-10
                    - cerebellum-161-97-117-96
                    - thalamus-217-76-59-241
      podAntiAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                app.kubernetes.io/name: gitea
                app.kubernetes.io/instance: gitops-gitea
            topologyKey: kubernetes.io/hostname
    topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app.kubernetes.io/name: gitea
            app.kubernetes.io/instance: gitops-gitea
    initContainers:
      resources:
        requests:
          cpu: 10m
          memory: 16Mi
        limits:
          cpu: 200m
          memory: 256Mi
