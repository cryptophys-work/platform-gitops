apiVersion: v1
kind: Service
metadata:
  name: spire-server
  namespace: spire
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app.kubernetes.io/name: spire-server
  ports:
    - name: grpc
      port: 8081
      targetPort: 8081
