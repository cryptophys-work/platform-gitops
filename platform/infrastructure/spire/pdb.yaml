apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: spire-server
  namespace: spire-system
spec:
  # Critical path: keep at least two servers during voluntary disruption (drain / rollout).
  minAvailable: 2
  selector:
    matchLabels:
      app: spire-server
