apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - minio-release.yaml
  - velero-release.yaml
  - ingress-minio-console.yaml
  - policy-exception.yaml
  - velero-policy-exception.yaml
  # backup-secrets and velero-schedules are in 50-backup-crs (need vault-backend ClusterSecretStore + Velero CRDs)
