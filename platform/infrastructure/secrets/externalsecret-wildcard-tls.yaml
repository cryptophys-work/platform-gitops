# Wildcard TLS secret replicated to all namespaces with Ingress resources.
# When ClusterSecretStore vault-backend is healthy these sync from Vault.
# Bootstrap fallback: kubectl copy from gitops-system secret.
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: gitops-system
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: apps-system
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: kube-system
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: observability-system
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: minio-system
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: kyverno-system
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: cqls-compute
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: longhorn-system
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: registry-system
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: vault-system
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
# ExternalSecret: TLS cert for ingress-system (nginx ingress controller default cert)
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-wildcard-tls
  namespace: ingress-system
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-wildcard-tls
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
---
# ExternalSecret: TLS cert for apps-gateway namespace
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cryptophys-wildcard-cert
  namespace: apps-gateway
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: cryptophys-wildcard-cert
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
      data:
        tls.crt: "{{ .tlscrt | b64dec }}"
        tls.key: "{{ .tlskey | b64dec }}"
  data:
  - secretKey: tlscrt
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.crt
  - secretKey: tlskey
    remoteRef:
      key: platform/tls/cryptophys-wildcard
      property: tls.key
