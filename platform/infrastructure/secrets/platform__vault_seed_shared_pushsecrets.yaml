apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: vault-seed-gitea-admin
  namespace: gitops-system
  labels:
    cryptophys.io/domain: platform
    cryptophys.io/component: vault-seed
    cryptophys.io/managed-by: flux
    cryptophys.io/layer: platform
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: gitea-ui-admin
  data:
    - match:
        secretKey: username
        remoteRef:
          remoteKey: apps/gitea/admin
          property: username
    - match:
        secretKey: password
        remoteRef:
          remoteKey: apps/gitea/admin
          property: password
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: vault-seed-gitea-helm-values
  namespace: gitops-system
  labels:
    cryptophys.io/domain: platform
    cryptophys.io/component: vault-seed
    cryptophys.io/managed-by: flux
    cryptophys.io/layer: platform
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: gitops-gitea-values
  data:
    - match:
        secretKey: values.yaml
        remoteRef:
          remoteKey: apps/gitea/helm-values
          property: values_yaml
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: vault-seed-registry-ci
  namespace: gitops-system
  labels:
    cryptophys.io/domain: platform
    cryptophys.io/component: vault-seed
    cryptophys.io/managed-by: flux
    cryptophys.io/layer: platform
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: registry-ci-credentials
  data:
    - match:
        secretKey: username
        remoteRef:
          remoteKey: apps/registry/ci
          property: username
    - match:
        secretKey: password
        remoteRef:
          remoteKey: apps/registry/ci
          property: password
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: vault-seed-argocd-admin
  namespace: apps-system
  labels:
    cryptophys.io/domain: platform
    cryptophys.io/component: vault-seed
    cryptophys.io/managed-by: flux
    cryptophys.io/layer: platform
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: argocd-initial-admin-secret
  data:
    - match:
        secretKey: password
        remoteRef:
          remoteKey: apps/argocd/admin
          property: password
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: vault-seed-argocd-server
  namespace: apps-system
  labels:
    cryptophys.io/domain: platform
    cryptophys.io/component: vault-seed
    cryptophys.io/managed-by: flux
    cryptophys.io/layer: platform
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: argocd-secret
  data:
    - match:
        secretKey: server.secretkey
        remoteRef:
          remoteKey: apps/argocd/server
          property: server_secretkey
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: vault-seed-grafana-admin
  namespace: observability-system
  labels:
    cryptophys.io/domain: platform
    cryptophys.io/component: vault-seed
    cryptophys.io/managed-by: flux
    cryptophys.io/layer: platform
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: grafana-admin-credentials
  data:
    - match:
        secretKey: admin-user
        remoteRef:
          remoteKey: apps/grafana/admin
          property: username
    - match:
        secretKey: admin-password
        remoteRef:
          remoteKey: apps/grafana/admin
          property: password
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: vault-seed-postgres-apps-ha
  namespace: postgresql-system
  labels:
    cryptophys.io/domain: platform
    cryptophys.io/component: vault-seed
    cryptophys.io/managed-by: flux
    cryptophys.io/layer: platform
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: postgres-apps-ha-app
  data:
    - match:
        secretKey: username
        remoteRef:
          remoteKey: apps/postgresql/apps-ha
          property: username
    - match:
        secretKey: password
        remoteRef:
          remoteKey: apps/postgresql/apps-ha
          property: password
    - match:
        secretKey: uri
        remoteRef:
          remoteKey: apps/postgresql/apps-ha
          property: uri
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: vault-seed-postgres-trustedledger
  namespace: postgresql-system
  labels:
    cryptophys.io/domain: platform
    cryptophys.io/component: vault-seed
    cryptophys.io/managed-by: flux
    cryptophys.io/layer: platform
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: postgres-trustedledger-ha-app
  data:
    - match:
        secretKey: username
        remoteRef:
          remoteKey: apps/trustedledger/postgres
          property: username
    - match:
        secretKey: password
        remoteRef:
          remoteKey: apps/trustedledger/postgres
          property: password
    - match:
        secretKey: uri
        remoteRef:
          remoteKey: apps/trustedledger/postgres
          property: dsn
