---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: rust-application-build
  namespace: tekton-system
  labels:
    cryptophys.io/language: rust
spec:
  description: Build, test, and sign Rust applications
  params:
    - name: repo-url
      type: string
    - name: image-name
      type: string
    - name: image-tag
      type: string
      default: latest
  workspaces:
    - name: shared-data
  tasks:
    - name: fetch-source
      taskRef:
        name: git-clone
      workspaces:
        - name: output
          workspace: shared-data
      params:
        - name: url
          value: $(params.repo-url)
    
    - name: test
      runAfter: [fetch-source]
      workspaces:
        - name: source
          workspace: shared-data
      taskSpec:
        workspaces:
          - name: source
        steps:
          - name: test
            image: rust:1.76-alpine
            script: |
              #!/bin/sh
              cd $(workspaces.source.path)
              cargo test --release
    
    - name: build-push
      runAfter: [test]
      taskRef:
        name: buildah-build-push-sbom
      workspaces:
        - name: source
          workspace: shared-data
      params:
        - name: IMAGE
          value: "registry.cryptophys.work/$(params.image-name):$(params.image-tag)"
