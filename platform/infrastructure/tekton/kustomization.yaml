apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Tekton infrastructure policies managed here (platform-level Kyverno rules).
# Application-layer Tekton resources are in apps-gitops (ArgoCD-managed).
resources:
  - policies/tekton-controller-harbor-images.yaml
  - crd-migration/tekton-crd-storage-migration.yaml
