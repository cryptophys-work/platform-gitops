apiVersion: v1
kind: ServiceAccount
metadata:
  name: observability.synthetic.checker
  namespace: observability
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: observability.synthetic.targets
  namespace: observability
data:
  check.sh: |
    #!/bin/sh
    set -eu
    now="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"
    fail=0
    while IFS='|' read -r name url expected_csv; do
      [ -n "${name}" ] || continue
      [ "${name#\#}" = "${name}" ] || continue
      code="$(curl -k -sS -o /dev/null -m 20 -w "%{http_code}" "${url}" || true)"
      matched=0
      IFS=',' 
      for expected in ${expected_csv}; do
        if [ "${code}" = "${expected}" ]; then
          matched=1
          break
        fi
      done
      IFS='|'
      if [ "${matched}" -ne 1 ]; then
        echo "${now} FAIL ${name} url=${url} expected=${expected_csv} got=${code}"
        fail=1
      else
        echo "${now} OK   ${name} url=${url} code=${code}"
      fi
    done <<'EOF'
    gitea|https://gitea.cryptophys.work|200
    harbor|https://registry.cryptophys.work|200
    argocd|https://argocd.cryptophys.work|200
    kyverno|https://kyverno.cryptophys.work|200,401
    vault|https://vault.cryptophys.work|307
    EOF
    if [ "${fail}" -ne 0 ] && [ "${STRICT_MODE:-false}" = "true" ]; then
      exit 1
    fi
    exit 0
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: observability.synthetic.checker
  namespace: observability
spec:
  schedule: "*/5 * * * *"
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/name: observability.synthetic.checker
        spec:
          serviceAccountName: observability.synthetic.checker
          restartPolicy: Never
          securityContext:
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: checker
              image: ghcr.io/go-gitea/gitea:1.25.4-rootless
              imagePullPolicy: IfNotPresent
              command:
                - /bin/sh
                - /config/check.sh
              env:
                - name: STRICT_MODE
                  value: "false"
              volumeMounts:
                - name: config
                  mountPath: /config
                  readOnly: true
              resources:
                requests:
                  cpu: 50m
                  memory: 64Mi
                limits:
                  cpu: 200m
                  memory: 128Mi
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                runAsNonRoot: true
                runAsUser: 1000
                runAsGroup: 1000
                capabilities:
                  drop:
                    - ALL
          volumes:
            - name: config
              configMap:
                name: observability.synthetic.targets
                defaultMode: 0555
