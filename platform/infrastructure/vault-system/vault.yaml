apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vault
  namespace: vault-system
spec:
  interval: 1h
  postRenderers:
    - kustomize:
        patches:
          - target:
              kind: Service
              name: vault
            patch: |
              - op: replace
                path: /spec/publishNotReadyAddresses
                value: false
          - target:
              group: admissionregistration.k8s.io
              version: v1
              kind: MutatingWebhookConfiguration
              name: vault-agent-injector-cfg
            patch: |
              apiVersion: admissionregistration.k8s.io/v1
              kind: MutatingWebhookConfiguration
              metadata:
                name: vault-agent-injector-cfg
                annotations:
                  cert-manager.io/inject-ca-from: vault-system/vault-injector-cert
  chart:
    spec:
      chart: vault
      version: 0.27.0
      sourceRef:
        kind: HelmRepository
        name: hashicorp
        namespace: flux-system
  values:
    global:
      enabled: true
      tlsDisable: true
    server:
      replicas: 3
      resources:
        requests: { cpu: 100m, memory: 128Mi }
        limits: { cpu: 300m, memory: 256Mi }
      nodeSelector:
        kubernetes.io/os: linux
        platform-ha: "true"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: platform-ha
                    operator: In
                    values:
                      - "true"
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - nexus-144-91-103-10
                      - cerebellum-161-97-117-96
                      - thalamus-217-76-59-241
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - nexus-144-91-103-10
            - weight: 50
              preference:
                matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - cerebellum-161-97-117-96
            - weight: 50
              preference:
                matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - thalamus-217-76-59-241
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app.kubernetes.io/instance: vault
                  app.kubernetes.io/name: vault
                  component: server
              topologyKey: kubernetes.io/hostname
      tolerations:
        # Cilium bootstrap taint
        - key: node.cilium.io/agent-not-ready
          operator: Exists
          effect: NoSchedule
        # Platform HA pool taint
        - key: cryptophys.io/pool
          operator: Equal
          value: platform-ha
          effect: NoSchedule
        # LLM inference node taint
        - key: cryptophys.io/role
          operator: Equal
          value: llm-inference
          effect: NoSchedule
        # Ray head node taint
        - key: ray-head
          operator: Exists
          effect: NoSchedule
        # DAO system node taint
        - key: dao-system
          operator: Exists
          effect: NoSchedule
        # Bridge system node taint
        - key: bridge-system
          operator: Exists
          effect: NoSchedule
        # Temporary taints
        - key: node.kubernetes.io/memory-pressure
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 300
        - key: node.kubernetes.io/disk-pressure
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 300
        - key: node.kubernetes.io/pid-pressure
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 300
        - key: node.kubernetes.io/not-ready
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 300
      dataStorage:
        enabled: true
        size: 1Gi
        storageClass: longhorn
      ha:
        enabled: true
        raft:
          enabled: true
          setNodeId: true
          config: |
            ui = true
            listener "tcp" {
              tls_disable = 1
              address = "[::]:8200"
              cluster_address = "[::]:8201"
            }
            storage "raft" {
              path = "/vault/data"
            }
            service_registration "kubernetes" {}
    injector:
      enabled: true
      replicas: 2
      certs:
        secretName: vault-injector-certs
      resources:
        requests: { cpu: 100m, memory: 64Mi }
        limits: { cpu: 250m, memory: 128Mi }
      # Increase liveness failureThreshold: cert-manager takes a few seconds to
      # populate vault-injector-certs at pod start → default threshold=2 kills pod too early
      livenessProbe:
        failureThreshold: 10
        initialDelaySeconds: 10
        periodSeconds: 5
      readinessProbe:
        failureThreshold: 5
        initialDelaySeconds: 5
        periodSeconds: 5
      startupProbe:
        failureThreshold: 20
        periodSeconds: 5
      # Rolling update in-place to avoid needing extra scheduling capacity
      strategy:
        type: RollingUpdate
        rollingUpdate:
          maxSurge: 0
          maxUnavailable: 1
      nodeSelector:
        kubernetes.io/os: linux
        platform-ha: "true"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: platform-ha
                    operator: In
                    values:
                      - "true"
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - nexus-144-91-103-10
                      - cerebellum-161-97-117-96
                      - thalamus-217-76-59-241
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - nexus-144-91-103-10
            - weight: 50
              preference:
                matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - cerebellum-161-97-117-96
            - weight: 50
              preference:
                matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - thalamus-217-76-59-241
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/instance: vault
                    app.kubernetes.io/name: vault-agent-injector
                    component: webhook
                topologyKey: kubernetes.io/hostname
      tolerations:
        - key: ray-cluster
          operator: Equal
          value: "true"
          effect: NoSchedule
        - key: cryptophys.io/pool
          operator: Equal
          value: platform-ha
          effect: NoSchedule
        - key: node.kubernetes.io/memory-pressure
          operator: Exists
          effect: NoSchedule
    # Dedicated UI Service (targets active Vault pod when activeVaultPodOnly=true).
    # Improves Web UI routing in HA and keeps navigation consistent with the leader.
    ui:
      enabled: true
      activeVaultPodOnly: true
      publishNotReadyAddresses: false
      serviceType: ClusterIP
      externalPort: 8200
      targetPort: 8200
