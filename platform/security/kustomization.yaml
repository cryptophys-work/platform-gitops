apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - falco-release.yaml
  - tetragon-release.yaml
  - gatekeeper-release.yaml
  # tetragon-tracing-policy.yaml deferred to 15-security-runtime-crs (TracingPolicy CRD not ready at dry-run)
