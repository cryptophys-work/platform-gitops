apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-code-forge-gitea-values
  namespace: gitea
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: platform-code-forge-gitea-values
    creationPolicy: Owner
  data:
    - secretKey: values.yaml
      remoteRef:
        key: apps/gitea/helm-values
        property: values_yaml
