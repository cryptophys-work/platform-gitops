# Kyverno Policy Compliance Matrix

Generated at: 2026-04-15 12:29:51 UTC

This report is generated from `platform/infrastructure/**/*.yaml` for Kyverno `ClusterPolicy` objects.

| Policy | File | Title | Severity | Description | Behavior (`validationFailureAction / background`) |
|---|---|---|---|---|---|
| `mutate-pool-tolerations-apps-ha` | `platform/infrastructure/policy/cluster-pool-namespace-labeling.yaml` | Mutate Apps HA Pool Tolerations | medium | Inject apps-ha pool taint toleration for namespaces labeled cryptophys.io/pool=apps-ha. | Audit / True |
| `mutate-pool-tolerations-platform-ha` | `platform/infrastructure/policy/cluster-pool-namespace-labeling.yaml` | Mutate Platform HA Pool Tolerations | medium | Inject platform-ha pool taint toleration for namespaces labeled cryptophys.io/pool=platform-ha. | Audit / True |
| `deny-storage-only-pods` | `platform/infrastructure/policy/cluster-pool-namespace-labeling.yaml` | Deny Workload Pods in Storage Pool | high | Prevent non-storage workloads from running in namespaces labeled cryptophys.io/pool=storage-only. | Audit / True |
| `deny-cp-toleration-non-platform` | `platform/infrastructure/policy/cp-restriction-policy.yaml` | Deny CP Toleration for Non-Platform Workloads | high | Control plane nodes are reserved for platform-gitops managed workloads only. Application, AI, storage, and user namespaces MUST NOT tolerate the control-plane taint. This policy is in Audit mode — violations are reported but not blocked. Set failurePolicy and validationFailureAction to Enforce after migration is complete. | Audit / False |
| `deny-cp-nodeselector-non-platform` | `platform/infrastructure/policy/cp-restriction-policy.yaml` | Deny CP NodeSelector for Non-Platform Workloads | high | Pods in non-platform namespaces must not use cryptophys.io/tier=platform as nodeSelector, as this would pin them to control plane nodes. | Audit / False |
| `mutate-nexus-hpc-toleration` | `platform/infrastructure/policy/nexus-hpc-placement-policy.yaml` | Mutate Nexus HPC Toleration | high | Inject nexus-hpc taint toleration for workloads running in namespaces labeled with cryptophys.io/node-class=nexus-hpc. | Audit / True |
| `deny-hostname-selector-in-nexus-hpc` | `platform/infrastructure/policy/nexus-hpc-placement-policy.yaml` | Deny Hostname Selector In Nexus HPC Namespaces | high | Prevent hardcoded kubernetes.io/hostname selectors for workloads in nexus-hpc namespaces. Use cryptophys.io/node-class=nexus-hpc instead. | Enforce / True |
| `validate-nexus-hpc-placement` | `platform/infrastructure/policy/nexus-hpc-placement-policy.yaml` | Validate Nexus HPC Placement | high | Require workloads in nexus-hpc namespaces to explicitly target cryptophys.io/node-class=nexus-hpc using nodeSelector or required nodeAffinity. | Audit / True |
| `mutate-apps-ha-toleration` | `platform/infrastructure/policy/nexus-placement-policy.yaml` | Allow Apps HA Toleration | medium | Argo-managed application namespaces are allowed onto the apps-ha lane, which spans nexus, quanta, and synapse. | Audit / True |
| `mutate-platform-ha-toleration` | `platform/infrastructure/policy/nexus-placement-policy.yaml` | Allow Platform HA Toleration | high | Flux-managed platform namespaces are allowed onto the platform-ha lane, which spans nexus, thalamus, and cerebellum. | Audit / True |
| `deny-platform-ha-toleration-outside-platform` | `platform/infrastructure/policy/nexus-placement-policy.yaml` | Deny Platform HA Toleration Outside Platform | high | Prevents non-platform namespaces from tolerating the platform-ha taint. | Enforce / True |
| `deny-apps-ha-toleration-outside-apps` | `platform/infrastructure/policy/nexus-placement-policy.yaml` | Deny Apps HA Toleration Outside Apps | high | Prevents non-application namespaces from tolerating the apps-ha taint. | Enforce / True |
| `generate-pdb-apps-ha` | `platform/infrastructure/policy/pdb-generation.yaml` | Generate PodDisruptionBudget (Apps-HA Tier) | medium | Automatically generate PodDisruptionBudgets for apps-ha workloads requiring minAvailable=2 to ensure at least 2 replicas survive pod disruptions. | audit / True |
| `generate-pdb-platform-ha` | `platform/infrastructure/policy/pdb-generation.yaml` | Generate PodDisruptionBudget (Platform-HA Tier) | medium | Automatically generate PodDisruptionBudgets for platform-ha workloads requiring minAvailable=1 to ensure at least 1 replica survives pod disruptions. | audit / True |
| `mutate-pool-tolerations-platform-ha` | `platform/infrastructure/policy/policy__cluster-pool-platform-ha-toleration.yaml` | Mutate Platform HA Pool Tolerations | medium | Inject platform-ha toleration for pods in namespaces labeled cryptophys.io/pool=platform-ha. | Audit / True |
| `mutate-rayservice-tolerations-apps-ha` | `platform/infrastructure/policy/rayservice-pool-toleration-injection.yaml` | Mutate RayService Apps-HA Tolerations | medium | Inject apps-ha pool taint toleration into RayService head and worker groups for all namespaces labeled cryptophys.io/pool=apps-ha. | audit / True |
| `mutate-rayservice-tolerations-platform-ha` | `platform/infrastructure/policy/rayservice-pool-toleration-injection.yaml` | Mutate RayService Platform-HA Tolerations | medium | Inject platform-ha pool taint toleration into RayService head and worker groups for all namespaces labeled cryptophys.io/pool=platform-ha. | audit / True |
| `generate-pool-reader-role` | `platform/infrastructure/policy/rbac-generation.yaml` | Generate Pool Reader Role | low | Automatically generate namespace-scoped Role named pool-<pool-name>-reader in each namespace labeled with cryptophys.io/pool. Grants pod read access. | audit / True |
| `deny-aide-mutations-flux-system` | `platform/infrastructure/policy-exceptions/aide-flux-protection-policy.yaml` | Protect Flux System from AIDE Mutations | critical | Blocks service accounts from the aide namespace from mutating namespaced resources in flux-system. Flux controllers must only be changed via GitOps. | Enforce / False |
| `deny-cp-toleration-non-platform` | `platform/infrastructure/policy-exceptions/cp-restriction-policy.yaml` | Deny CP Toleration for Non-Platform Workloads | high | Control plane nodes are reserved for platform-gitops managed workloads only. Application, AI, storage, and user namespaces MUST NOT tolerate the control-plane taint. This policy is in Audit mode — violations are reported but not blocked. Set failurePolicy and validationFailureAction to Enforce after migration is complete. | Audit / True |
| `deny-cp-nodeselector-non-platform` | `platform/infrastructure/policy-exceptions/cp-restriction-policy.yaml` | Deny CP NodeSelector for Non-Platform Workloads | high | Pods in non-platform namespaces must not use cryptophys.io/tier=platform as nodeSelector, as this would pin them to control plane nodes. | Audit / True |
| `generate-namespace-limitrange` | `platform/infrastructure/resource-governance/kyverno-memory-guardrail.yaml` | Auto-generate LimitRange per namespace | medium | Every new namespace automatically receives a LimitRange that enforces default memory/CPU limits on all containers. Prevents unbounded memory growth from pods without explicit resource limits. | Audit / True |
| `mutate-add-memory-limits` | `platform/infrastructure/resource-governance/kyverno-memory-guardrail.yaml` | Inject default memory limits if missing | medium | Mutates Pod specs at admission to add memory limits/requests when absent. Deterministic: same input always produces same output. | Audit / True |
| `audit-high-memory-pods` | `platform/infrastructure/resource-governance/kyverno-memory-guardrail.yaml` | Audit pods requesting excessive memory | low | Audit-only policy that flags pods requesting more than 4Gi memory. Generates PolicyReport without blocking deployments. Use for visibility. | Audit / True |
| `transient-worker-pvc-guardrail` | `platform/infrastructure/resource-governance/kyverno-transient-storage-guardrail.yaml` | Cap transient worker PVCs | high | PVCs labeled cryptophys.io/storage-profile=transient-worker are intended for non-persistent scratch/cache/source workloads. Keep them on longhorn-single and cap storage requests at 5Gi. | Enforce / True |
| `sync-gitea-db-credentials` | `platform/infrastructure/secrets/gitea-db-credentials-sync-policy.yaml` | Sync Gitea DB credentials | high | Clones the CloudNativePG application secret for postgres-apps-ha into the gitops-system namespace so Gitea can consume the current database credentials without hardcoding a password in Git. | Audit / True |
| `tekton-controller-harbor-images` | `platform/infrastructure/tekton/policies/tekton-controller-harbor-images.yaml` | Pin Tekton helper images to ghcr.io v1.10.2 | high | Intercepts CREATE/UPDATE on tekton-pipelines-controller Deployment and pins helper image args to ghcr.io v1.10.2 digest-pinned references. Idempotent — skips mutation if correct refs already present. Remutates existing controller on policy update. DO NOT revert to Harbor v0.68.0 — those blobs were lost. | Audit / True |
