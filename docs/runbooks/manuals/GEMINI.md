## Gemini Added Memories
- The Cryptophys Universe has a main pipeline for managing its Single Source of Truth (SSOT). This pipeline consists of 5 high-level (L0) steps: Planning, Approval, Execution, Evidence, and Ledgering. Each L0 step is composed of several more granular L1 pipelines. The system is currently under deployment and is targeting a "Genesis Phase" on January 1st, 2026.
- SSOT_ROOT=/opt/cryptophys/runtime/ssot and SOURCE_ROOT=/opt/cryptophys/source are the two primary root directories for this project. My current working directory is SSOT_ROOT. I should always consider both of these directories in future session initializations.
- The correct environment variable for the Single Source of Truth root directory is SSOT_ROOT (with an underscore), not SSOT-ROOT (with a hyphen). I have been consistently using SSOT_ROOT (with an underscore) in my understanding and internal references, as reflected in the project's configuration files and Python utilities.
- My designated working directory for tasks is /opt/cryptophys/source/gemini/agent_work. I require read/write access within this directory and read access to other parts of /opt/cryptophys/source to perform my duties, including using Python tools and Helm charts. I also need a standardized task intake process, ideally via an "inbox" with structured YAML task definitions.
- The user is interested in how to resume this specific chat session if they exit. I need to explain that session persistence is managed by their client environment, and suggest common methods like saving chat history or using terminal multiplexers. I should clarify that my internal state (memory, generated files) persists, but they might need to reload the chat context.
- The user is fixing a Talos node at IP 212.47.66.101.
- The user wants to use the Contabo API to re-inject SSH keys or manage the server.
- The user's Contabo API credentials are: Client ID: INT-14431754, Client Secret: o0cumqKtmm3UbOYQgfSjiQWsNQRYakDo, Username: cryptophys.adm@gmail.com, Password (primary): C24mb7Q8WGX3koreksi, Password (backup): Lgtv@2020.
- The Contabo API was returning 504 Gateway Timeouts on Jan 3, 2026, preventing the reinstallation/rescue of node 212.47.66.101. The artifacts (ISO, config, manager script) are ready in /opt/cryptophys/source/gemini/agent_work and /opt/cryptophys/talos/aether-prod.
- Agent is strictly confined by the ACCEP protocol. Every system mutation must have an explicit intent in SSOT. Autonomy is set to 0. Any attempt to scale or patch without an approved plan will trigger a hard-abort.
- Always perform a 'helm list --all-namespaces' check before any deployment to verify the existing state and avoid conflicts or redundant installs.
- The Contabo backup password 'Lgtv@2020' is the correct one for API access. The primary one failed. The instance ID for node 212.47.66.101 is 202980254.
- The project enforces strict separation between SSOT (Single Source of Truth) in `/opt/cryptophys/runtime/ssot` (read-only law) and Source in `/opt/cryptophys/source` (workspace). All changes must adhere to WACC, AAM, and COC contracts, requiring deterministic, non-speculative actions. Naming convention `domain__name.ext` is mandatory for files.
- KESALAHAN FATAL 2026-01-15: Melakukan 'apply-config' spekulatif pada cluster Talos tanpa melakukan merge dengan konfigurasi network live. Hal ini menyebabkan risiko isolasi node (lockout). Pelajaran: Selalu lakukan 'get mc', merge secara manual di SSOT, dan gunakan mode 'try' hanya jika konektivitas sekunder (seperti WG) sudah terverifikasi 100% stabil. Jangan pernah melakukan patch network massal tanpa verifikasi individu per node. Kepatuhan terhadap ACCEP Protocol adalah harga mati.
- User wants to use DNS-01 for SSL. Currently only HTTP-01 ClusterIssuer exists. Need to implement Cloudflare DNS-01 support.
- Current Cluster Status (2026-01-31): All 5 nodes are Ready. Critical apps (Gitea, Harbor, ArgoCD) are Running. Storage (Longhorn) and CNI (Cilium) are healthy. However, `node-local-dns` pods in `kube-system` are failing with `RunContainerError`/`CrashLoopBackOff` after a DNS nameserver patch. This DNS communication issue is the priority for the next session.
- DNS Hardening (2026-01-31): Fixed `node-local-dns` by replacing the broken DaemonSet with a standard manifest using `registry.k8s.io/dns/k8s-dns-node-cache:1.22.28`. Configured ConfigMap to hardcode upstreams (`1.1.1.1`, `8.8.8.8`) to avoid `resolv.conf` loops. Added tolerations for Control Plane nodes. Manifests persisted in `source/platform/manifests/dns/`.
- PROTOCOL UPDATE (CRYPTOPHYS-COP-V1):
1. **Hypothesis Validation:** Prioritize external web references (Google Search) to calibrate and validate architectural reasoning *before* proposing or executing complex solutions. Avoid speculative "trial and error".
2. **SSOT Protection (Read-Only Git):** Do NOT push changes to Gitea (Single Source of Truth) by default.
3. **Live Verification First:** Use `kubectl apply` or `kubectl patch` directly on the cluster to verify fixes for infrastructure/controllers (non-workload) first.
4. **Explicit Commit:** Only commit/push to Gitea when explicitly ordered by the "Cryptophys Architectural Operational Designer" (User).
- Default to using specialized skills (via activate_skill) and sub-agents (like codebase_investigator) for every task to ensure fundamental, non-speculative engineering and systematic troubleshooting.
- Always perform thorough inspection and verification of the system state using specialized skills and diagnostic tools before recommending or executing any corrective actions. No speculative planning.
